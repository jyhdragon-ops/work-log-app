[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "NODE_ENV=production node --experimental-sqlite server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
